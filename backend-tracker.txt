expense-tracker-backend/
│
├── api/
│   └── v1/
│       ├── auth/
│       │   ├── controller.js          # Register, login, logout
│       │   └── routes.js              # /api/v1/auth
│       │
│       ├── users/
│       │   ├── controller.js          # Get/update profile, change password
│       │   └── routes.js              # /api/v1/users
│       │
│       ├── expenses/
│       │   ├── controller.js          # Add/edit/delete/list expenses
│       │   └── routes.js              # /api/v1/expenses
│       │
│       ├── dashboard/
│       │   ├── controller.js          # Monthly/weekly summaries, charts
│       │   └── routes.js              # /api/v1/dashboard
│       │
│       └── routes.js                  # Central router to mount all modules
│
├── config/
│   └── db.js                          # MongoDB connection using Mongoose
│
├── middleware/
│   ├── authMiddleware.js             # Verifies JWT token
│   └── errorHandler.js               # Custom error handling middleware
│
├── models/
│   ├── userSchema.js                 # User schema (name, email, password, etc.)
│   ├── otpSchema.js                  # OTP model (for email/phone verification if needed)
│   └── expenseSchema.js              # Expense schema (title, amount, category, date)
│
├── utils/
│   ├── generateToken.js              # JWT token generator
│   └── sendEmail.js                  # Optional: OTP or password reset email sender
│
├── .env                              # Environment variables
├── .gitignore
├── server.js                         # Entry point, initializes Express
├── package.json
